// Set VITE_GITHUB_REPO in your .env.local (dev) or Vercel environment variables (prod)
// Format: "username/repo-name"  e.g. "davidoligney/meridian"
export const GITHUB_REPO = import.meta.env.VITE_GITHUB_REPO ?? '';
